# osf-project
OSF Academy front-end project.

The project demostrates an HTML templates/scss layout of a e-commerce website, uses gulp for build.

Build: `npm run build`
Dev launch: `npm run dev`
Preview on local server: `npm run start`
